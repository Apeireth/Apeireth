# 实测验证台账 (Live Verification Ledger)

> **给谁看**：任何要写"X 已经验证过/还没验证"的人——写进文档、代码注释、
> commit message 之前先查这张表。**目的：别让后人反复测试已经测过的东西，
> 也别把没测过的当测过的。**
>
> **口径**：只有"当时真跑了、有可复现命令/提交记录/输出"的才上绿表。
> 挂账 = 明确未测或测不了，写明原因。本表随每次新实测更新（更新规则见 §4）。
> 更细的产品能力对账见 `docs/03-reference/absorption-2026-09.md`（RA-15 吸收批）。

## 1. 已实测（绿）——不要再重测，除非要复核

### 1.1 真模型 E2E（DeepSeek = openai-compatible，唯一实测 provider）

| # | 项 | 何时 | 证据 | 复现命令（需 key） |
|---|---|---|---|---|
| 1 | provider 能力级 + factory 级 smoke | 2026-09-08 | `crates/engine/provider/tests/openai_compatible_live.rs`（2 个 `#[ignore]` live） | `$env:OPENAI_API_KEY='…'; $env:APEIRETH_OPENAI_URL='https://api.deepseek.com/v1'; $env:APEIRETH_OPENAI_MODELS='deepseek-v4-flash'; cargo test -p apeireth-provider --test openai_compatible_live -- --ignored` |
| 2 | CLI canonical 双轮对话（会话连续性 + trace/provider） | 2026-09-08 | 会话记录（commit `e2ab8213` 前后） | `apeireth chat "…" --model deepseek-v4-flash --session (uuid)`（同上 env） |
| 3 | shell 工具全闭环（提议→冻结→approve→执行→回灌→总结，真模型真调用） | 2026-09-08 | commit `edbd7694`；Trace 实证 `CapabilityDispatched/CapabilityCompleted(succeeded)` | `$env:APEIRETH_ENABLE_SHELL=1` + chat 触发工具 → `apeireth approve --session … --approval …` |
| 4 | organ W1 反事实推演 + W2 因果图（live LLM） | 2026-09-08 | `crates/engine/organ/tests/organ_live_llm.rs`（44s 全过） | `cargo test -p apeireth-organ --test organ_live_llm -- --ignored` |
| 5 | Council 7-advisor → Approved | 2026-09-08 | `crates/foundation/orchestration/tests/council_live.rs`（5.0s） | `cargo test -p apeireth-orchestration --test council_live -- --ignored` |

### 1.2 桌面 HTTP 主链路（gateway 协议 = 桌面前端实际调用面）

| # | 项 | 何时 | 证据 | 说明 |
|---|---|---|---|---|
| 6 | `/v1/chat/completions` 真模型多轮 + 会话连续性（turn2 正确回忆 turn1 事实） | 2026-09-10（新 key） | 本会话探针输出；commit `1a265600` | 桌面同款 payload：`{model, messages:[{role:'user',content}], session_id, stream:false}`；`served_by=provider.openai-compatible`；panel/sessions 持久化 |
| 7 | `stream:true` SSE 帧（init/content/final + apeireth 元数据） | 2026-09-10 | 同上 | **token 级真流式**（语义与证据见 #14；本条早期"整段后分帧"表述作废） |
| 8 | 错误路径 JSON body（`error` + `session_id`） | 2026-09-10 live + 库级测试更早 | `canonical_openai_compatible_entry.rs::the_gateway_reports_an_openai_compatible_missing_credential_as_unavailable` | 502/503 均带体；"裸 502"是早期探针读流姿势问题，不是产品缺陷 |
| 9 | `/v1/models` 按 id 去重（`minimax-m3` 2→1，`minimax-m3-thinking` 保留） | 2026-09-10 | live 实测 + `the_gateway_models_list_dedupes_ids_shared_across_providers` | 根因：anthropic 插件默认端点即 MiniMax Anthropic 兼容网关 |

### 1.3 装机 / 卸载 / 配置注入（Windows NSIS）

| # | 项 | 何时 | 证据 | 复现命令 |
|---|---|---|---|---|
| 10 | 装机 E2E 全链 11/11（安装→装机侧车真聊天→gateway /health→桌面冒烟→孤儿复现→卸载零残留） | 2026-09-10（新包 `CB318756…`） | `frontend/companion-desktop/scripts/install-e2e.ps1` | `$env:OPENAI_API_KEY='…'; $env:APEIRETH_OPENAI_URL='https://api.deepseek.com/v1'; $env:APEIRETH_OPENAI_MODELS='deepseek-v4-flash'; pwsh frontend/companion-desktop/scripts/install-e2e.ps1` |
| 11 | NSIS 卸载器侧车进程检查（运行中侧车被卸载器杀掉，零残留） | 2026-09-08 | `installer.nsh` hook + install-e2e 孤儿场景回归 | 同 #10 |
| 12 | Settings provider 配置注入侧车环境（apply → 重启换端口 → `/v1/models` 出现注入模型 → 重复 apply 不重启） | 2026-09-10 | `frontend/companion-desktop/src-tauri/tests/supervisor_lifecycle.rs::provider_env_reaches_the_backend`（**真后端、无需 key**） | `cargo test -p companion-desktop --test supervisor_lifecycle`（src-tauri 内） |
| 13 | 高级能力旋钮注入侧车（P9：`apply_backend_config` 开 shell → 重启 → `/v1/tools/list` 出现 `"name":"shell"` 且 `permission:"granted"` → 重复 apply 不重启；合并 apply 单次重启契约） | 2026-09-10 | `supervisor_lifecycle.rs::capability_env_reaches_the_backend`（**真后端、无需 key**）+ `capability_env_pairs_match_canonical_knobs_and_fail_closed`（fail-closed 单元测试） | `cargo test -p companion-desktop --test supervisor_lifecycle` |
| 14 | **token 级真流式增量**（provider SSE → runtime sink → gateway 逐帧直通；审批/错误以显式终帧终止流） | 2026-09-10 | live 实测（DeepSeek，210 帧 ~23ms 逐帧到达）；`complete_streaming_forwards_incremental_deltas`（provider，mock SSE 顺序+合成同语义）；`the_gateway_streams_incremental_deltas_when_requested`（gateway，role<hel<lo<[DONE] + usage/元数据）；`openai_compatible_stream_ends_with_pending_approval_frame`（审批流式契约） | `curl -N -X POST …/v1/chat/completions -d '{"model":"deepseek-v4-flash","messages":[{"role":"user","content":"…"}],"stream":true}'`（需 key） |
| 15 | **侧车启动与 CWD 解耦**（System32 CWD 启动：侧车存活 + 存储落 `%LOCALAPPDATA%\Apeireth\data\` + /health 200） | 2026-09-28 | `supervisor_lifecycle.rs::sidecar_stores_land_in_app_data_regardless_of_cwd`（真后端、无需 key）+ install-e2e "hostile-CWD boot" 步骤（**用户真机点击流首发现的 P0 bug 的回归**） | `pwsh frontend/companion-desktop/scripts/install-e2e.ps1`（无 key 可跑，聊天探针自动 SKIP） |

### 1.4 DSH 参考采纳批（P0×5 / P1×6，2026-09-12）

> 全部为「自动化测试已证」口径；需要人工点击流验证的部分仍在 §2 挂账 #2。

| # | 项 | 证据 | 复现命令 |
|---|---|---|---|
| 16 | **网关统一错误帧契约**（`{"error":{"message","code","solution"}}`，8 码中文解决方案目录；SSE 错误帧同形状） | `crates/adapters/gateway` 单测（error_frame/error_codes）+ 4 处既有断言同步 + `tests/admin_config.rs`；**真机实证**：失效 key 的 vendor 401 经新契约上浮为可读错误（认证往返完成） | `cargo test -p apeireth-gateway` |
| 17 | **`/v1/admin/config` 无重启热配置**（model/base_url/api_key 热更新下一请求生效，非法 config 拒绝且旧值不变，GET 打码回显） | `gateway/tests/admin_config.rs` 2 个集成测试（mock server + 内存凭证 store） | `cargo test -p apeireth-gateway --test admin_config` |
| 18 | **会话级模型记忆 + 会话级权限预设三态**（read_only 拒写/执行、standard 沿用审批、full 免审批留日志；老库 JSON 自动迁移不丢数据；预设钩子已接生产 CLI 治理管线） | `runtime-assembly/tests/session_permission_preset.rs` + `sqlite_session.rs` 迁移测试 + CLI 接线 commit `e7da5809` | `cargo test -p apeireth-runtime-assembly -p apeireth-cli` |
| 19 | **审批载荷带命令文本**（`command_text` / `arguments_summary`，现有字段全保留） | `crates/engine/runtime/src/canonical/approval.rs` 序列化单测 | `cargo test -p apeireth-runtime` |
| 20 | **桌面钥匙串 IPC + 无重启应用（404/405 回退重启）+ 工作区目录**（keyring 只进系统钥匙串不落盘；绝对 env > 锚定 > 相对 env 视为无效的新 store 契约） | companion-desktop 30 单测 + `supervisor_lifecycle.rs` 8 集成测试（**真 sidecar**，含 `explicit_env_store_paths_take_priority`） | `cargo test`（src-tauri 内） |
| 21 | **前端数据层 + 6 个新组件 + SettingsView/App.svelte 集成**（审批卡显示命令文本、错误解决方案横幅、会话模型/预设选择器、工具生命周期卡、斜杠菜单、工作区选择器） | `npm run check` 0 errors（警告 5 条全为既有）+ `npm run test` 7/7 | `cd frontend/companion-desktop; npm run check; npm run test` |
| 22 | **本批新包装机回归 + 真聊天探针**（新 store 锚定契约的 hostile-CWD 启动 + 卸载钩子零残留 + 桌面冒烟 + 装机侧车真聊天） | install-e2e 17/17（2026-09-12，新 NSIS 包，key `sk-3f4a…3544`） | `$env:OPENAI_API_KEY='…'; $env:APEIRETH_OPENAI_URL='https://api.deepseek.com/v1'; $env:APEIRETH_OPENAI_MODELS='deepseek-flash'; pwsh frontend/companion-desktop/scripts/install-e2e.ps1` |
| 23 | **审批载荷旧 blob 迁移修复**（升级前持久化的审批记录缺 `command_text`/`arguments_summary` 导致整会话加载失败 → 两个字段加 `#[serde(default)]`；真机用户会话 `5f05690d` 实测修复后加载 200） | commit `f69cd68d` + 回归测试 `approval_view_serialized_before_command_text_still_deserializes` + **真实数据验证**（用户 `%LOCALAPPDATA%\Apeireth\data\sessions.sqlite3` 副本 → `GET /v1/sessions/5f05690d…/settings` 200） | `cargo test -p apeireth-runtime`；复核：gateway 指 DB 副本查 settings |
| 24 | **真机三连修复**（① 前端错误码透传：`toRuntimeError` 曾丢弃后端 `code`/`solution` 导致横幅永远显示通用文案 → `RuntimeError.backendCode/solution` 透传 + `describeError` 识别；② Windows 凭据跨进程持久化：keyring v3.6 的 `CRED_PERSIST_ENTERPRISE` 在微软账户机器上对其他进程不可见 → 直写 Credential Manager `CRED_PERSIST_LOCAL_MACHINE`，跨进程探针两遍验证 + cmdkey 互操作验证；③ 热应用/重启契约：capabilities/模型列表等非热可应用变更必须重启 → 按 spawn 快照分类，30 单测 + 8 集成全绿） | commit `d610bb2a`；真机注入链验证：凭据库 key 已送达侧车并完成 DeepSeek 认证往返（vendor 401 因 key 被撤销，非链路问题） | `cargo test`（src-tauri 内）+ 两遍 `--ignored` keychain 跨进程探针 |
| 25 | **旋钮整改批**（① filesystem/search 执行许可默认放行与 repo 同待遇 + `APEIRETH_DISABLE_LOCAL_READ_TOOLS=1` 逃生门（ENABLE 兼容、DISABLE 赢），语义矩阵 4 测试；② 会话级审批策略 `approval_remember`（standard+记住 → 同 (会话,工具) 已批准后 RequireApproval→Allow、**Deny 永不绕过**、进程内记忆）+ `GovernanceHook::approval_resolved` 回调 + pipeline 转发；③ 桌面 4 档策略选择器 + 全局预设接入会话创建 + 认知深度预设档；④ 文档对账修复 7 文件） | commits `7e3690a6`/`6a2d134a`/`03dc636a`/`cac36848`；全量 workspace 测试绿 + cli 46 passed + governance/runtime/assembly 全绿 + svelte-check 0 errors + 7/7；**装机 e2e 17/17**（2026-09-12，含真聊天） | `cargo test --workspace`；`npm run check && npm run test`（companion-desktop）；`pwsh frontend/companion-desktop/scripts/install-e2e.ps1` |
| 26 | **协作者批整合**（origin/main 领先 56 提交 = 记忆 v2.2 生产收口 + 行为链安全 Guard 新 crate `apeireth-guard` 接生产治理管线（4 新旋钮 `APEIRETH_GUARD_DATASET_ENABLED/PATH`、`APEIRETH_GUARD_ML_MODE/MODEL`）+ 认知 vnext 生产接线；快进整合后全量验证） | 2026-09-19 快进至 `b86a89d5`（PR #15）；`cargo check --workspace --all-targets` + `cargo test --workspace` + 桌面 src-tauri 测试 + svelte-check + 7/7 **全绿**；新包装机 e2e **17/17**（含真聊天，新 key `sk-a426…3d17`）；凭据库已更新为新 key；新包已装回用户机 | 同 #25 |
| 27 | **认知模块预算修复**（真机 2026-09-19：认知深度"深度"档 judge+council 单轮合法消耗 8 次侧调用恰好顶满旧预算 8，judge 一要求重试第 9 次调用即 `BudgetExceeded` 炸毁整个回合且归码 internal → ① `DEFAULT_MAX_MODULE_INVOCATIONS` 8→24；② 预算耗尽**优雅降级**：该模块本 pass 跳过、候选回复照常提交、会话事件留痕 `module_budget_degraded`（0 装不假装评审通过）；回归测试 `budget_exhaustion_degrades_the_module_instead_of_aborting_the_turn`） | commit `c9174eba`；runtime 全量 + assembly/orchestration 全绿；**深度档真机复现干净通过**（judge+council 双开聊天 rounds=1 正常完成）；新包 install-e2e **17/17**（含真聊天） | `$env:APEIRETH_COGNITIVE_JUDGE=1; $env:APEIRETH_COGNITIVE_COUNCIL=1; apeireth chat …`；`cargo test -p apeireth-runtime --test cognitive_module_abi` |
| 28 | **2026-10-06 真机修复批次**（全天 11 项，主人桌面实测逐个抓出：① judge retry 预算耗尽 Stop→best-effort；② judge 空响应/坏 JSON→降级放行；③ resolve 5min 超时+晚到失败不静默+定时重拉 inbox 自救；④ notice/error 分离（状态提示不再挂"请重试"）；⑤ 切策略后提示未决审批不追溯；⑥ GBK 回退解码（date 不乱码）；⑦ 新错误码 `review_rejected`/`turn_not_converged`；⑧ 敏感名单补 GeminiApiKey/.git-credentials 后缀/产品 DB；⑨ shell 双引号命令 raw_arg 直传（引号不再被吃）；⑩ 会话级工作区（新会话自动选择器+切换自动重根）；⑪ 认知深度档实测延迟标注） | 完整清单见 `engineering-log-2026-10-06.md`（每项带 commit）；全量 workspace + 桌面 + svelte-check + 7/7 全绿；装机 e2e 17/17 多次复跑；全套边界测试由应用内模型实测（shell 非沙箱实锤、敏感名单复测通过） | 见 engineering-log §5 |
| 29 | **`presence_state` 事件落地 heuristic_v0**（00-PHILOSOPHY §10 契约 v0：gateway 内状态合成模块 + 事件上现有 SSE 总线；significance 三档；initiative 预算 ≤3 次/天；60s 心跳 + 无交互衰减 baseline；契约文档 §8a 先行） | 2026-09-22 live 探针：起 `target/debug/apeireth.exe gateway serve --port 8080`，`curl -sN -m 70 /v1/apeireth/events` 实收 2 帧 `presence_state`（约 60s 一拍，shape 逐字段与契约一致：`type/at/pad/dominant/intensity/stance/breath/significance/source{kind:heuristic_v0}`，空闲态 `dreaming_consolidation` + baseline PAD）；presence.rs 12 个单测覆盖 shape/分级/频率 | commits `20cf3d1b`/`487b7141`/`fa011d07`；`cargo test -p apeireth-gateway` 全绿 + `clippy --all-targets -D warnings` 零警告 + `cargo test --workspace` 3406 passed；复现：`./target/debug/apeireth.exe gateway serve --port 8080` 后 `curl -sN -m 70 http://127.0.0.1:8080/v1/apeireth/events` |
| 30 | **K3 前端产品化批次视觉与集成验收**（② T0 聊天壳三栏主从+个性化背景；③ 治理卷宗四 tab；④ Ctrl+K 命令面板 19 条+打断近似；⑤ 底部状态条四指标；⑥ 记忆卷宗主从化+日记纸面空态；⑦ presence.ts 改订 presence_state 契约——具名帧订阅/显影分级/T0 余烬/T1 光晕/T2 PAD 映射） | 2026-09-22/23 主会话逐项 CDP 截图亲审（真后端为主）：会话列表两档照明可读、三栏主从、治理四 tab 真实数据（grants 3 条/traces 46/audit 100）、面板拼音过滤、状态条安静态「SSE 已连接·记忆 34」与杀后端后「重连中+不可用」级联、记忆主从真数据、日记纸面空态、**真实 60s 心跳帧驱动余烬 amplitude 0.65→0.2**；打断按钮走 mock 慢流式（诚实标注）；两轮打回返工（列表承托/会话头竖排+金晕+衬线行宽）复核通过 | commits `5021157e`…`7026a3a7`（14 支）；`npm run check` 0/0 + `npm run test` 15/15 + `cargo test -p apeireth-gateway` 全绿；复现：起 gateway+vite 后用 `artifacts/k3-*.mjs` CDP 脚本（scratch 未入库） |
| 31 | **视觉走查 P0 修复批（4 项，全部为前端壳修复，不触后端）**：① 未选会话空态指引 `.bg-hint` 加 chip 承托（原裸 text-shadow 在 night 黑洞亮环/essence 山脉上不可读，与同款 `.bg-caption` 对齐）；② essence 浅色主题对比校准（面板 0.76→0.85、描边 0.10→0.16、白雾遮罩 0.52→0.38、金家族 #8a7355→#a08050 家族提亮、bone 透明度上调；规范 + 令牌 JSON 已同步回写，数值待主人视觉终验）；③ 设置页保存栏 `grid-column: 1 / -1`（原被 grid 自动摆进第 2 行第 1 列 200px 窄条，按钮竖排文字截断，CDP 布局诊断实证）；④ 治理卷宗四 tab 共享 `GovManifestPending`（capabilities 恒 null 时原无限转圈；12s 预算内转圈、超时给诚实「清单迟迟未到」说明——非不支持，连接恢复 15s 轮询自动载入） | 2026-09-23 逐项 CDP 截图亲审（无网关态 + essence/night 主题）：`fix1-night-hour2.png` 黑洞亮环上 chip 文案清晰可读；`fix2-essence-hour22.png` 卡片描边/文字分明、山脉景深层次恢复；`fix3-settings.png` 保存栏通栏横排、说明单行完整；`fix4-gov-pending.png` 15.5s 超时态诚实说明呈现；`npm run check` 0/0 + `npm run test` 15/15；证据 `artifacts/fix*.png` + `fix-verify-shots.mjs`（scratch 未入库） | 复现：vite dev 后 `node artifacts/fix-verify-shots.mjs` |
| 32 | **视觉走查 P1 修复批（5 项）**：① 设置页主题卡片缩略图区分度（essence 缩略图曾用不透明渐变完全盖住底图显空白——改半透明叠加透出真图；night 加黑洞金色吸积环预览、日光加暖阳、纸面加注册表横线纹理、深海加冷光柱、林海加林冠绿晕）；② 对话主页列表空态门槛放宽（原要求 capabilities !== null——离线全新用户整栏空白无任何入口，现空态契约 + 「开始第一段对话」CTA 离线可用，发送时离线如实弹错误横幅）；③ 工具抽屉错误态不再显示「已装配工具 N 个」计数（空列表 + 计数 = 矛盾信息）；④ 眉题可读性（drawer-head/home 眉题 9.5-10px/bone-30/0.34-0.5em → 10px/bone-42/0.26em，远看不再是噪点）；⑤ 工具抽屉离线诚实态（复用 GovManifestPending：清单未到 ≠ 不支持，原「未实现」结论的判定补 `capabilities !== null` 门）；另查证挂账：首启向导仅在 `isDesktop()`（Tauri）下触发属刻意平台门（向导写钥匙串/配侧车，浏览器端无 IPC 不可用），非缺陷 | 2026-09-23 逐项 CDP 截图亲审：`p1-theme-cards.png` 六主题缩略图两两可辨、essence 透出真图；`p1-home-empty.png` 离线空态 CTA 呈现；`p1-tools-error.png` 错误态计数消失；`p1-tools-pending.png` 15s 超时诚实态呈现；`npm run check` 0/0 + `npm run test` 15/15；证据 `artifacts/p1-*.png` + `p1-verify-shots.mjs`/`p1-tools-recheck.mjs`（scratch 未入库） | 复现：vite dev 后 `node artifacts/p1-verify-shots.mjs` |
| 33 | **主人真机反馈修复批（2026-09-23，四项）**：① composer 的 M/◯ 弹出面板被压成 85px 宽破框（`.panel` 的 `max-width:100%` 相对 87px 定位父级 `.composer-caps`——改 `max-width:min(356px,calc(100vw-80px))`；全窗糊化是 `#scrim.show` 的 `backdrop-filter:blur(8px)` 聚焦层，面板修好即为设计本意；附带删 `.panel` 自身 `backdrop-filter:blur(20px)` 及 essence 更强雾面覆写——父子嵌套 backdrop-filter 在 Edge 有合成隐患，面板底 `--ap-shell-elevated` 近不透明，删之无视觉差）；② 空心主题删除（day/ocean/forest/paper 无任何实现，点了无反应违反 0 装——从 `THEME_CATALOG`/`VALID_THEMES`/`Theme` 联合类型/App.svelte `THEME_PINYIN` 删除，`base.css` 休眠亮色令牌块一并清掉；config 里残留值经 `resolveTheme` 守卫回落 heritage-void；**补全已列 §6 梯队 6 backlog**，主人指示「最后我们都是要做的」）；③ 设置保存栏从 `.settings-layout` 通栏固定黑带（2549 宽屏上比例失调）移入 `.settings-content` 内容流末尾——每个分页最下面的保存卡片，不跨 grid 列、比例随 900px 内容列收敛；④ 浅色/深色模式方向**未擅自定案**——§5 增补 #9 等主人拍板（提议 essence=浅色模式做到位，Archive 纸面恒纸面不动；**当日已拍板，见 #34**） | 2026-09-23 CDP 截图亲审（1600×900 全新 profile）：`p3-theme-cards.png` 主题卡片只剩三套（遗产星空/Essence/深空舰桥）、说明文案同步；`p3-savebar-models.png`/`p3-savebar-memory.png` 保存栏为内容流末尾紧凑卡片；`p3-model-panel.png`/`p3-ctx-panel.png` 面板 356px 行内容清晰；2549×1352 复验 `p2-model-panel.png`/`p2-ctx-panel.png` 同结论；`npm run check` 0/0 + `npm run test` 15/15（theme-system 断言同步更新：空心主题必须回落默认）；证据 `artifacts/p3-*.png` + `p3-verify-shots.mjs`（scratch 未入库） | 复现：vite dev 后 `node artifacts/p3-verify-shots.mjs`（2549 复验用 `p2-repro.mjs`） |
| 34 | **主人真机反馈第二批（2026-09-23，Essence=浅色模式当日拍板 ✅）**：① **M/◯ 面板「看不清」真凶不是配色是层叠**——`.composer-caps` 的 `backdrop-filter` 生成层叠上下文，把 `.panel` 困在 z-auto 层，`#scrim`(z17) 盖在其上：`elementFromPoint` 实证面板中心命中 scrim——**文字被 scrim 毛玻璃糊化 + 点击全被拦截（点模型行=关面板，选不中）**。修：`.composer-caps { z-index:18 }`（仍在 .shell z5 上下文内，抽屉 30+/persona 31/32 不受影响），复验面板中心命中 `.search`；② essence 浅色对比提升（`.panel h2/.kv/.bar-head` bone-42→bone-68、`.model`→bone 实色、`.panel .wb-empty`→bone-68、essence 下 `#scrim.show` 毛玻璃 8px→4px 减奶雾）；③ **保存栏主题化**：`--surface-1`（全主题无定义）→ `--surface-2`（根深/essence 浅双模式都有值），浅色模式下保存栏不再是一块深色违和黑带；④ 拍板落定：Essence=浅色模式做到位（不另起主题）、Archive 纸面恒纸面——§5-9 转 ✅，梯队 6 同步 | 2026-09-23 CDP 截图亲审：`p6-essence-model-panel.png` 面板文字锐利（当前模型/MiniMax-M3/搜索框/模型行清晰）、`p6-essence-ctx-panel.png` 同；`p6-essence-savebar.png` 浅色亮卡 + 金色保存按钮；`p6-night-savebar.png` 深色卡片保持；像素采样 + `elementFromPoint` 双实证（修复前 2:1 对比度 / hitIsScrim=true，修复后命中面板 `.search`）；`npm run check` 0/0 + `npm run test` 15/15；证据 `artifacts/p4-*.png`/`p6-*.png` + `p4-diag.mjs`/`p5-stacking.mjs`/`p6-verify-shots.mjs`（scratch 未入库） | 复现：vite dev 后 `node artifacts/p6-verify-shots.mjs`（层叠实证 `p5-stacking.mjs`） |
| 35 | **Essence 浅色模式全抽屉审计（2026-09-23，11 视图连拍）**：history / memory / diary / tools / governance×4 tab / status / logs / settings 全部过 essence 截图亲审——10 个视图达标；**治理卷宗 1 处文字不可读**：其底色是刻意的深舱注册表（Deep-Ops 渐变，§B-12 拍板档调性，不随 essence 变浅——与 Archive 纸面同理），但 essence 全局 bone/muted/faint/text 是浅底深字，在深舱底上加载态与 12s 超时诚实态文字全糊。修：`html[data-theme='essence'] .governance-view` 子树作用域还原文字令牌为深底浅字（取值=:root 默认），档调性不动 | 2026-09-23 CDP 截图亲审：`p7-essence-*.png` 11 张审计底账；修复后 `p7b-essence-gov-fixed.png`（13s 超时态）标题/说明/图标/四 tab 标签在深舱底上全部可读；`npm run check` 0/0 + `npm run test` 15/15；证据 `artifacts/p7-*.png` + `p7-audit-shots.mjs`/`p7b-gov-recheck.mjs`（scratch 未入库） | 复现：vite dev 后 `node artifacts/p7-audit-shots.mjs`（修复复核 `p7b-gov-recheck.mjs`） |
| 36 | **Essence 浅色真数据验收（2026-09-23，gateway :8080 活连）**：`apeireth.exe gateway serve --port 8080` 活进程 + vite :1420，essence 下 10 视图真数据连拍——治理四 tab（审计 46 条执行轨迹真账、授权/守卫/审批筛选器与说明文字）在深舱底上全部可读（#35 修复实锤）；记忆卷宗 34 条真记忆纸面可读；系统状态真数据（4802 词元/MiniMax-M3/网关 18ms/提供商 4792ms）；工具注册表诚实空态（后端未注册工具，0 装不假装）；日志诚实空态；历史=客户端本地会话（新 profile 为空，预期行为）。**本轮零新缺陷，无代码改动** | 2026-09-23 gateway `/health` = ok 实测；`p8-essence-*.png` 10 张真数据截图亲审；证据 `artifacts/p8-*.png` + `p8-live-shots.mjs`（scratch 未入库） | 复现：`./target/debug/apeireth.exe gateway serve --port 8080` + vite dev 后 `node artifacts/p8-live-shots.mjs` |
| 37 | **四主题补全批（2026-09-23 主人指示「最后我们都是要做的」）：day（日光）/paper（纸面）/ocean（深海）/forest（林海）真实现，替换 #33 删空心留下的 backlog**。① 接线回归：`Theme` 联合类型/`VALID_THEMES`/`THEME_CATALOG` 四条目（照 P1 批 678b8e4e 精修版目录原文）、App.svelte `THEME_PINYIN` + app-root 四个 class 绑定；② 背景=纯 CSS 渐变无照片资产（设计决定）：日光暖阳渐变、纸面注册表横线纹理、深海冷光柱、林海林冠绿晕，四主题入 `STATIC_BG_THEMES`（静态背景无 WebGL），场景层/presence 同隐藏，浅底主题（essence/day/paper）挂浅色 #vignette 晕影；③ 令牌：tokens.css 四块完整令牌（day 暖白面板族/金家族沿用 essence 校准值，paper 中性灰白+金降饱和，ocean/forest 深底只染面板族/分隔线/info、骨白与存在金继承 :root 不破金色纪律）+ base.css 四块 Pattern 令牌；④ essence.css 浅色覆写 89 条规则扩到 day/paper（每行选择器 essence→三行组，REST 逐行保留；治理卷宗深舱底文字令牌随 .governance-view 块同步还原），12 处金字面量 rgba(160,128,80,X)→color-mix(in srgb, var(--ap-gold) X%, transparent)；motion.css 雾面四组覆写扩 day/paper；⑤ **事故与修复（教训入档）**：批量 sed 扩展把选择器列表断行成裸 `html[data-theme='day']`（整个 day 页面被 `display:none`——白屏根因；essence 幸免仅因默认主题不写 data-theme 属性），还原 HEAD 后用 Python 逐行展开重做（`{` 行尾：前两行改逗号、末行开块），花括号配平 73/73 校验过；⑥ paper 走查修复：底色 #f0eee9→#e9e6de/#d2cfc6、横线 0.035→0.05、面板 alpha .86→.92、--ap-line .13→.16（原面板与底色同灰、横线不可见）；⑦ theme-system.mjs 断言翻转：四主题必须合法/静态背景/目录条目原样保留 | 2026-09-23 `p9-theme-shots.mjs` 四主题×主页/设置/记忆卷宗 12 张 CDP 截图亲审：日光暖调渲染正常（白屏已消）、纸面横线纹理可见+面板分界清晰、深海舱蓝/林海沉绿层次可读；`npm run check` 0/0 + `npm run test` 15/15；证据 `artifacts/p9-*.png` + `p9-theme-shots.mjs`/`p9e-day-diag2.mjs`/`p9f-day-diag3.mjs`（scratch 未入库） | 复现：vite dev 后 `node artifacts/p9-theme-shots.mjs`（day 白屏诊断 `node artifacts/p9f-day-diag3.mjs`） |
| 38 | **档案调深舱反色批（2026-09-23 主人真机反馈：「深色模式下，记忆和日记却仍然是白色背景」）**：「纸面恒纸面」拍板（#34）收窄为浅色语境——深色主题下记忆卷宗/日记转深舱档案调。① tokens.css 档案令牌（`--ap-register-archive-*`）原 :root 一套纸面值写死，新增伴随值令牌（ink-60/55/40/35/26/14、ink-solid、surface-2/3）并把深色覆写块挂 `html:not([data-theme])`（深空舰桥=默认主题，applyDocumentTheme 对它**移除** data-theme 属性，写 `[data-theme='night']` 永不命中——与 #37 sed 白屏同款的默认主题陷阱）+ heritage-void/ocean/forest：石墨深底 #16181d 族、骨白墨 #e8e0cc 族、幽灵编号 #2e3339、条目深卡反浅卡、文字级强调换亮金 #ecc271（原下沉金 #8a6d1f 深底不可读）；金色纪律不动（accent 金配墨字两态通用，ink-solid 不覆写）；② MemoryView/DiaryView 全部硬编码伴随值改走令牌（rgba(38,38,42,*)×7、#ecebe9/#e5e4e2、金字上墨字 #26262a×2——这些是深色下不可读的元凶）；③ 浅色系（essence/day/paper）保持纸面原值不动 | 2026-09-23 `p10-archive-shots.mjs` 七主题×记忆/日记 14 张 CDP 截图亲审：深四（night/heritage/ocean/forest）档案石墨深底+骨白墨全部可读、金字/橙标清晰；浅三（essence/day/paper）纸面原值无回归（中途抓到自己两处验收口径错误已修：night 不能用无参数 URL——默认回落 heritage-void；essence 也必须显式 `?theme=essence`）；`npm run check` 0/0 + `npm run test` 15/15；证据 `artifacts/p10-*.png` + `p10-archive-shots.mjs`（scratch 未入库） | 复现：vite dev 后 `node artifacts/p10-archive-shots.mjs` |
| 39 | **v2 现役 18 crate 源码能力清单审计**（不读 docs、只对源码实证：逐 crate 职责/公开入口、逐域能力三档状态（真实现/部分/stub·0 装）、stub 点全清单带行号、测试形态计数 `#[ignore]` 分布、源码内部文档不一致）→ 产出差距文档 §8 附录 A | 2026-10-06 源码复核（三路并行子代理 + 首手 grep/read 补漏）：`crates/*/*/tests/*.rs` 共 **92** 个；`#[ignore]` 标记 **47** 处（`rg -n '#\[ignore' crates/` 计数），全部集中真 key/真硬件 E2E：`engine/organ` 35 / `engine/provider` 7 / `foundation/orchestration` 3（`council_live.rs` 需 `OPENAI_API_KEY`）/ `engine/memory` 1（benchmark）/ `engine/perception` 1（xcap 需真显示器）——**即 `cargo test` 默认不跑任何真模型用例**；唯一全量 stub crate = `apeireth-sdk`（`STUB_MODE=true` `client.rs:112`）；**推翻**"BM25 缺失"旧结论（`memory/src/hybrid_search.rs:137,292` BM25+RRF 真实现） | 复核命令：`rg -n "unimplemented!|todo!" crates/`、`rg -n "0 装" crates/`、`rg -n "#\[ignore\]" crates/`；清单落档 `v1-vs-v2-capability-gap-audit-2026-10-06.md` §8 |
| 40 | **构建缓存清理 + 完好性验证**（主人机器空间告急；清理**仅可重建产物**，源码/数据/已装应用一律不动）：① `target/debug/incremental` 75.0 GB；② `src-tauri/target`（桌面壳独立构建目录）14.9 GB；③ `target/**/*.pdb` 调试符号 **1768 个 / 108.4 GB**（单个 180-200 MB，是反复重编堆出的旧版本，是体积主因）。**共回收 187.7 GB**（C 盘可用 202.6 → 390.3 GB） | 2026-10-06 清理后**完好性实测**：`cargo test --workspace --no-run` **Compiling 次数 = 0**（零重编，仅测试二进制重链），**39.8 秒**完成；`cargo test --workspace` = **129 suite / 3406 passed / 0 failed / 19 ignored**，与清理前基线**逐项一致**；`target` 下残余 `.pdb` = 0。**重建代价结论：下次 `cargo test` 只需约 40 秒，不是全量重编** | 复现：`Get-ChildItem target -Recurse -Filter *.pdb \| Remove-Item -Force`（.pdb 可随时由 cargo 重建；`RUST_BACKTRACE=1` 的符号化会触发重链） |
| 41 | **W2 接线批首批：记忆召回三组接线 + 第三层调用链审计**（① **typed 写读对称修复**：写侧 `typed_sink` 早已落库 commitment/persona/relation，读侧 `typed_recall` 恒 `None` = **入库永不召回**；接 `SqliteTypedMemoryRecallSource`（episodes 槽不接——episodic 候选已由 scoped_memory 供，且该槽吃 `SqliteMemoryStore` 与生产池 `SqliteBackend` 不同型），补 `with_identity` 激活 persona 写，**默认开** + `APEIRETH_DISABLE_TYPED_RECALL=1` 逃生门，身份 `APEIRETH_PERSONA_ID/SUBJECT_ID` 默认 `apeireth`/`local-user`；② **语义向量阶段真实现**：`OpenAiCompatibleEmbeddingProvider`（OpenAI `POST /embeddings` transport + 纯解析 + 7 单测），`APEIRETH_EMBEDDING_URL/MODEL` 双缺=词法回退不变、**只设其一=启动报错**（fail-loud 大声失败）、双全=构造注入，`APEIRETH_EMBEDDING_KEY` 可选；③ **proactive_recall 补旋钮**：`APEIRETH_ENABLE_PROACTIVE_RECALL=1` 让已接线的 proactive 路径可达（默认关不变）；④ 三审推翻两条旧结论：**BM25/hybrid 检索经 `MemoryCoordinator` 早已接线**（"0 引用=未接"是名字扫描陷阱——crate 内部互调也是生产路径），`consolidation` 真相 = API 级已接（`coordinator.rs:840 run_consolidation`）、触发点未接 | 2026-10-06 源码调用链实证（差距审计 §7.1 三次复核表）+ 回归测试：provider `embeddings::*` 7/7、cli `production_knobs` 新增 5（默认关对照/逃生门/身份稳定/fail-loud 半配/双 bootstrap 冒烟）；效果级证据链：本批 CLI 测试证 env→接线映射 + 既有 `memory_provider_e2e`（typed 召回覆盖效果）+ `cognitive_convergence_vertical` T9（语义阶段效果）；**全量 129 suite / 3418 passed / 0 failed / 19 ignored（基线 3406 +12 = 新测试数）+ clippy 0 警告** | `cargo test -p apeireth-cli`；`cargo test -p apeireth-provider`；语义阶段 live 见挂账 #8 |
| 42 | **审核方复核结论整合 + B1 自纠 + 当期基线刷新**（独立复核批在交接包 §2.7 落 17 项判定：14 ✅ / 2 ⚠️ / 1 子结论 ❌）：① **❌ 推翻我的 B1**——"unimplemented!() 真宏调用 7 处（全在 SDK）"不成立：过滤注释行后**真宏调用 = 0**，8 处文本命中全是文档注释提及（7 在 `sdk/client.rs`、1 在 `plugin/perception.rs:23`）；已逐行亲验确认（教训第四条：**数数也要对源码**——注释里的宏名不是调用；0 装纪律双向性的又一实例）；② ⚠️×2 记录在案：C4 旋钮枚举漏 `APEIRETH_REASONING_ENABLED/MODEL_FILTERS/TAG`（真实开关）、D1 差 1 条 VCP 注释提及（`protocol:498` 的 `__oneRingMeta`，非实现）；③ 当期态基线全量刷新 3406→**3418**（README×2、README.zh-CN×3、INSTALL、ROADMAP×2、SECURITY、maintenance-guide、HANDOFF 共 8 文件 11 处；**带日期的真账行按约定保留原值**）；④ 记文档债：测试数在多份文档硬编码会随每批失真，W8 文档治理时统一改为"以台账为准"指针式 | 2026-10-06 逐行亲验（`client.rs` 30/37/457/528/602/693/713 全为 `//!`/`///`）+ 全仓库过滤非注释行实证 0 真调用；与复核批独立结论一致 | 复现：`Select-String -Path (Get-ChildItem crates -Recurse -Filter *.rs).FullName -Pattern 'unimplemented!\(\|todo!\('` 后逐行滤注释 |
| 43 | **W2 记忆闭环批（consolidation/reflexion/memory_injection 三模块接线）+ 挂账核销四审**（① **接线**：consolidation 触发点（writeback AfterTurn 跑 `run_consolidation`、洞察稳定 ID 落库幂等、`APEIRETH_ENABLE_CONSOLIDATION` 默认关）、reflexion 失败闭环（新模块 `cognitive.reflexion`：TurnStart 教训注入 + AfterTurn 消费 Judge **显式**非 Pass 判定沉淀 `DecisionRejected` 并 RuleCritic 即时蒸馏——Judge 未开=无信号=不记录，绝不文本猜失败；`APEIRETH_ENABLE_REFLEXION` 默认关 + `APEIRETH_REFLEXION_DIR`）、memory_injection 反幻觉格式（donor 编号证据清单 + 「禁止说『我记得我们以前聊过』」，`APEIRETH_ENABLE_MEMORY_INJECTION` 默认关）；② **效果测试咬出 2 个真缺陷并修**：FakeMemory 半真 fake（治理视图恒空→从 episodes 如实派生）、**consolidation 洞察自我增殖**（派生记忆含 "resolved" 被再提炼出级联副本——实测 2 轮 4 个 ID→修复后只从 user/assistant/tool 原始证据提炼+幂等断言）；③ **挂账核销**：thought_cluster 钉死（v2 `cluster_store.rs` = v1 `thought_cluster` 完整改名移植，**从真缺口移出**）；HybridCognitiveRouter/ToolSynthesizer **改判纯愿景**（v1 亦无）；community/experiment_field/onering/**Docker 容器沙箱**/HTTP·WS 客户端群（http-client+ws_v1+bus-l4+lark+voice+update）/**整 crate 双洋葱+Kani 形式化证明**（`apeireth-onion` L0-L5 全六层）六组 legacy 源码级实证 v1 真实现（差距审计 §7.2 四审表，逐项带文件行号）；`jimmy` 死指针删除（删前复验） | 2026-10-06 全量 **129 suite / 3425 passed / 0 failed / 19 ignored**（3418+7=新测试数）+ clippy `-D warnings` 0 警告；新测试：memory 注入格式×3、assembly consolidation/reflexion/格式切换×3、cli 旋钮×1；legacy 标准=逐个 read 实现体（不读名字） | `cargo test -p apeireth-runtime-assembly`；`cargo test -p apeireth-cli`；legacy 证据见差距审计 §7.2 四审表 |
| 44 | **挂账核销收官批 + council 假绿洞封堵**（① **抓到测试自身 0 装漏洞**：`council_live` 断言把 `DeferToHuman` 包进"合法出口"，而 7 advisor 全失败恰好汇聚成它——**撤销 key 下 0.67s 假绿通过**（真跑历史 5.0s），降级出口冒充 live；封堵：测试开头加最小真 LLM 往返探针，通道死显式炸（修复后同 key 下**诚实红**，探针报 401——洞封实证）；② **live E2E 三支实跑结果**：provider 0/2、organ 0/2 = **key 尾号 3d17 已被 DeepSeek 撤销**（vendor 401 "Your api key: ****3d17 is invalid"）——**桌面 app 真聊天同样会 401，需主人换新 key**；council 1/1 系假绿（不算核销，见①）；③ **shell 可达实测重放**（新探针 `tools/tests/shell_reach_probe.rs`，`#[ignore]` 可复现）：`cmd /C type %SystemRoot%\win.ini` exit=0 **全文可见** = 执行底座可读工作区外全盘零拦截（非沙箱实锤；W1 落地后反转为回归锚点）；④ **Option 默认关配置全量排查**（W2 陷阱扫全仓 47+65 字段）：行为开关类已全部有旋钮（本批四补后闭合）；剩 4 个可达性观察项（materializer/extractor 增强槽、Judge 独立模型槽、orchestrator 安静时段、RuntimeConfig 三参数 builder-only）——非能力开关，记表不加面 | 2026-10-06 实跑记录：council 堵洞前后同 key 对照（假绿 0.67s → 诚实红 0.40s @ probe）；shell 探针 stdout 实录 win.ini 全文；provider/organ 401 报文存档于测试输出 | `cargo test -p apeireth-orchestration --test council_live -- --ignored`（现诚实红）；`cargo test -p apeireth-tools-canonical --test shell_reach_probe -- --ignored --nocapture`（现绿=非沙箱）；live E2E 换新 key 后重跑 provider/organ 两支 |
| 45 | **W2 §4.1 dreaming 接线 + W4①② 凭据收口 + W3 报告 §9 修正 + Cargo.lock 补漏**（① **dreaming**（三个设计决策点拍板，理由见 `memory/src/dream_wiring.rs` 头注）：D1 = `LlmMetaThinker`（装配层 `canonical/dream_llm.rs`，经 plugin `LlmFactory` 桥真思考，私有 current-thread runtime 隔离同步口）+ `FallbackMetaThinker` 确定性降级链（降级产出带 `[LLM 不可用(…), 降级]` 留痕——只降级不枪毙）+ `DeterministicMetaThinker` 兜底（memory 层零 LLM 依赖）；D2 = **CLI 显式命令 `apeireth dream`**（免旋钮=显式授权，`--session/--limit/--date`）；D3 = `dream_and_journal` 胶水：苏醒 `report.to_markdown()` 落 `FileDiaryStore`（`<data>/diary`，source=`dream`），日记失败不伪装成功（Err 显式声明"周期已成但写档失败"）。新增 12 测（memory dream_wiring 5 / assembly dream_llm 4 / cli parse 3）。② **W4 凭据收口**：`CredentialApprovalHook`（真 GovernanceHook：高危凭据→`RequireApproval` + `approval_resolved` 会话记忆，approval_remember 语义）+ `ApprovalMemoryGate`（一份 `ApprovalMemory` 两张脸，喂 `GatedCredentialsStore`）+ `FileAuditSink` 真落档（JSONL+fsync，IO 失败只降级计数）+ `AuditContext::with_salt` 占位真身化（`name_hash_salted`），新增 10 测。③ **W3 报告 §9 修正**：沙箱 v1 翻案对账（`legacy/frozen/apeireth-sandbox/real.rs` 1228 行真接 Docker daemon HTTP API——原判"骨架"过度概括）+ `community.triage` 低估更正。④ **Cargo.lock 补漏**（`f11ade68`，修上笔 `--locked` CI 洞） | 2026-10-10 亲跑：`cargo test -p apeireth-memory -p apeireth-runtime-assembly -p apeireth-cli` **全绿 0 失败**（含新 12 测）；`cargo test -p apeireth-credentials` 92 passed；clippy `-D warnings` 0 警告（修 SubagentRole Copy 冗余克隆）；doc-caliber 守门 0 违规 | `cargo test -p apeireth-memory --lib dream_wiring`；`cargo test -p apeireth-runtime-assembly --lib canonical::dream_llm`；`cargo test -p apeireth-cli --bin apeireth`；真机做梦：`apeireth dream --session <sid> --limit 20`（LLM 未配时纯规则路径同样可跑） |
| 46 | **真缺陷 #4：思考型模型 max_tokens 预算链（council live 咬出，3 处修 + 1 处 fail-loud 加固）+ 挂账 #9 核销**（① **病理**：deepseek-v4-flash 等思考型把 `max_tokens` 全喂 `reasoning_content` → `content` 空壳（教训注释早在 `canonical_openai_compatible.rs` adapt_request：500 必截断/2048 仍空 1/3/4096 稳——但兜底**只在 max_tokens 缺省时**生效，显式小预算处处踩雷）；② **咬出链**：council_live 探针 `Some(64)` 必死（curl 同参侥幸 33 token reasoning 塞进=非确定性）→ 顺藤摸出 `advisors_llm.rs:118` 顾问 `Some(512)` 同雷（7 顾问全灭→`map_llm_error_to_deny` 映射 `advisor error`→Stop→聚成 `Vetoed{by:Safety}`）+ 当日新写的 dream `LlmMetaThinker Some(400)` 同雷；③ **修复**：探针 64→2048 / 顾问 512→**None**（走 provider 4096 兜底）/ dream 400→2048；**加固**：`openai_chat::parse_response` 对「空 content + length 截断」**fail-loud**（BadResponse 带行动指引，不发假空壳；finish=stop 的空 content 不拦=真输出）；④ **语义澄清**：`map_llm_error_to_deny`（错误→Stop+`advisor error` 明标）系有意设计（比 Abstain 诚实、fail-closed），未动 | 2026-10-10 三支 live 全实跑：**provider 2/2（1.73s）/ organ 2/2（43.39s）/ council 1/1（11.81s 真 7 顾问并行）**；修复前后对照：探针红(空 content)→绿(真内容)；顾问预算修后 verdict 从 advisor-error 聚合 → 真实裁决链（`DeferToHuman@PerformanceAdvisor timeout` = 诚实出口）；单测 orchestration 212 / provider 100 / dream_llm 4 全绿 0 回归 | `cargo test -p apeireth-orchestration --test council_live -- --ignored --nocapture`；`cargo test -p apeireth-provider --test openai_compatible_live -- --ignored`；`cargo test -p apeireth-organ --test organ_live_llm -- --ignored`（临时 key 仅进 env 不入库，掩码 `****409cc3`；换 key 重跑即复现）。**遗留观察**：PerformanceAdvisor 单顾问超时→DeferToHuman（思考型延迟 vs 顾问超时窗），并入 council 旋钮改造批调 |
| 47 | **council 改造批：每轮评审器 → 决策环节顾问**（① **语义改造**（主人 2026-10-10 拍板「一般运行时不要一直用，只在特定决策环节用」）：触发位点定案 **A** 升级/部署批准、**B** 高危操作 L3+（与洋葱合流）、**C** 待裁冲突批量裁决（空闲时，与 dream/Nightwatch 同昼夜节律）、**D** 显式咨询——A/B/C 随对应模块（upgrade_cycle ratification / 洋葱 L3-L5 / 守夜人）接入，**D 本批落地** `apeireth council "<议题>"`（显式授权免旋钮；真顾问裁决链；未配 LLM **fail-loud** 不造假裁决）；一般轮次不常开（深度档 = 深思直答）。② **旋钮**（主人拍板「做成旋钮让用户自己决定」）：`APEIRETH_COUNCIL_ADVISORS=N`（1-7，**默认 3** = 7→3；规范序取前 N，Safety 恒首位）+ `APEIRETH_COUNCIL_TIMEOUT_MS`（单顾问超时，默认 30000）；库默认 `per_advisor_timeout` 10s→30s = **#46 遗留观察闭环**。③ **0 复制**：抽共享 `llm_factory_from_env`（dream/council 同源工厂分派） | 2026-10-10 亲跑：cli+orchestration **282 passed / 0 failed**（含 `parse_council_topic` 2 测 + dream/council 共享工厂回归）；clippy `-D warnings` 0 警告（顺修 `unreachable pattern`：`CouncilVerdict` 恰三变体，兜底臂冗余）；doc-caliber 0 违规 | `cargo test -p apeireth-cli --bin apeireth`；`cargo test -p apeireth-orchestration`；真机咨询：`apeireth council "是否允许 X"`（需 `OPENAI_API_KEY`+`APEIRETH_OPENAI_MODELS`）；旋钮复现：`APEIRETH_COUNCIL_ADVISORS=7` / `=1` |
| 48 | **W1 P3 纵深批：凭据绊线真接线 + 口令模式 + 系统修改命令守门**（W1 沙箱轻量档已拍板，纯 Rust 纵深先行，AppContainer FFI=§2.1 下批）。① **咬出接线假账实例**：`ToolGuardrail::scan_and_sanitize_output`（5 类凭据绊线）**零生产调用**（grep 全仓仅定义+导出）——IMPLEMENTED ≠ PRODUCTION WIRED 现形；**真接线**：`ShellTool::format_result` stdout/stderr 双双过绊线，命中即脱敏截断 + 结果带 `credential_tripwire {triggered, leaked_kinds}`（shell 输出是唯一全权出口，2026-10-06 真机边界测试结论）。② **口令模式补全**（§2.2 设计点名）：新增 `PlaintextPassword` 类 + `password[ :=]值` 手写扫描（不引 regex），值截断到行尾；匹配用 **ASCII 快速比较**（自查纠偏：`to_lowercase` 字节偏移在中文输出下会切片错位）。③ **守门扩展**（§2.3）：`forbidden_commands` +12（`reg add/delete/import`、`sc create/config/delete`、`netsh set/add/delete/reset`、`attrib +s/+h`）+ **netsh 写动词组合规则**（抓 `netsh advfirewall set` 动词后置形态——纯子串表抓不到；show/display 只读放行）。④ 诚实边界：口令扫描单次一个命中段（不声称全扫）；守门否定式保守接受误报；`APEIRETH_SHELL_SANDBOX` 旋钮与 AppContainer 同批（**不做死开关**） | 2026-10-10 亲跑：tools 全套 **220 passed / 0 failed**（165+5+20+23+7；新增 4 测：口令截断含中文上下文回归 / 无赋值放行 / 系统修改命令矩阵 / netsh 组合规则）；clippy `-D warnings` 0 警告 | `cargo test -p apeireth-tools-canonical`；复现咬点：`grep -r scan_and_sanitize_output crates/`（修复前仅定义+导出）；真机验收待 P2 AppContainer（`shell_reach_probe` 断言反转同批） |
| 49 | **W1 沙箱本体落地：AppContainer 全链路 + 双探针反转实锤（墙是真的）**。① **四连环真机排障**（12 组合诊断矩阵逐一定位，防复蹈核心资产）：**a)** 进程创建**校验 profile 注册**——纯派生 SID 无 profile 背书报误导性"找不到文件"(os error 2)，`CreateAppContainerProfile`（幂等：ALREADY_EXISTS→派生同一 SID）修复（mode 4→7b 对照实验定案）；**b)** AC 零访问 token 无 PATH 搜索 → 父侧解析绝对路径作 `lpApplicationName`；**c)** 环境块三连 203（乱序→排序 / 缺 `CREATE_UNICODE_ENVIRONMENT`→补旗 / 瘦块→档案六件变量 `USERPROFILE/APPDATA/LOCALAPPDATA/HOMEDRIVE/HOMEPATH/USERNAME`，E4 无 AC 对照 + E5 全量对照定案）；**d)** `\\?\` 扩展前缀 cwd 掉 %WINDIR% → 授权+lpCurrentDirectory 剥前缀。② **真实边界发现**（ACL 验尸）：`win.ini` 自带 `ALL APPLICATION PACKAGES (I)(RX)` 继承（系统文件自我放行，**非墙缺口**）——**墙的边界 = 用户空间**（`C:\Users\*` 零 AAP ACE），旧探针 win.ini 断言注为史证、对偶探针改打用户空间界外文件。③ **落地件**：`process/appcontainer.rs`（profile 身份/属性表 RAII/DACL 合并授予/零 capability=断网）、windows.rs 沙箱 spawn（fail-closed 绝不裸跑）、shell.rs 双态诚实声明 + `sandbox` 徽标 + 保护默认开、cli `APEIRETH_SHELL_SANDBOX` 旋钮（=0 显式裸跑）、超时测试换无网长命令（ping 秒死=断网实锤转正为测试）。④ **fail-closed 链条实证**：沙箱开+能力不达 → 拒绝执行（测试期真实触发） | 2026-10-10 亲跑：tools+cli **287 passed / 0 failed**（tools 229 含新增：幂等 acquire/探针一致性/授予矩阵/双态声明/默认开旋钮/要求集/断网/界外零访问/工作区读写）；clippy `-D warnings` 0 警告；**双探针 live 实录**：历史探针 win.ini 全文可见 exit=0（非沙箱史证）vs 沙箱对偶 **界外读 exit=1 空输出**（墙成立）+ 里写成 exit=0 `sandbox-ok` | `cargo test -p apeireth-tools-canonical --test shell_reach_probe -- --ignored --nocapture`（双探针同跑对照）；`cargo test -p apeireth-tools-canonical --lib appcontainer`（矩阵单测）；排障矩阵复现：`diagnose` 组合实验已拆（结论固化于 `appcontainer.rs` 模块头注） |
| 50 | **W2 §4.2 partner 羁绊接线 + principles 有意不接处置 + W1 涟漪断言反转收尾**（① **partner 已接线**（五件验收门齐）：新模块 `cognitive.partner_bond` —— TurnStart 关系状态注入（`PromptOverlay::system`，羁绊阶段/深度/演化次数供语气与信任校准）+ AfterTurn 纯确定性演化（`touch`+`Bond::evolve` 回写 `PartnerStore`，0 LLM）；旋钮 `APEIRETH_ENABLE_PARTNER_BOND=1` 默认关；身份口径 = `subject_id`（`APEIRETH_SUBJECT_ID` 跨会话稳定，typed 身份同源）；0 假装：存储现 `InMemoryPartnerStore` 进程内（重启即散），持久 sqlite 为后续层（trait 落点已备）。② **principles 有意不接**（处置非遗漏）：头注 **P2 生产接线禁令**自证（生产激活必须走 governance/approval 路径，禁 Runtime 直连 store/artifact/master token）——未来 = governance/approval 专项设计（与 §5 红线 #4 同场评估）。③ **W1 涟漪断言反转第三处收尾**：`canonical_shell_approval_e2e.rs` 硬编码 `"Unsupported"` 快照改为**对齐能力报告本身**（报什么测什么，跨平台 CI 自洽；Enforced 时加验审批卡沙箱徽标——墙的存在批准前可见） | 2026-10-10 亲跑：assembly+cli **204 passed / 0 failed**（含 `partner_bond_tests` 3：默认关/注入文本/存储效果可见）；clippy `-D warnings` 0 警告；W2 五件验收门逐项落 | `cargo test -p apeireth-runtime-assembly`（partner 三测 + E2E 徽标断言）；旋钮复现：`APEIRETH_ENABLE_PARTNER_BOND=1` 跑 chat 看 TurnStart overlay「【关系状态】」 |
| 51 | **W2 §4.3 双件落地：morphology 检索深度自适应 + education Dx-Check 工具注册**（① **morphology 已接线**（真身纠偏：在 `engine/organ/morphology.rs` **非 memory**——交接包细节误差，B1 式）：落点=检索前置 `MemoryRecallModule::with_limit` 自适应（`morphology_recall_limit()` 纯函数：`crawl_budget` [1,6] 只**收紧**不放大、绝不越配置上限）；旋钮 `APEIRETH_ENABLE_MORPHOLOGY_RECALL=1` 默认关；温度旋钮 `APEIRETH_MORPHOLOGY_TEMPERATURE` 原生已在（`env_temperature()` 非法回 1.0）。② **education 已注册**：`EducationTool`（简单 invoke 型 ToolCapability，与 SearchTool 同契约）+ `EducationModule`（`module.tool.education`）+ production 注册；旋钮 `APEIRETH_ENABLE_EDUCATION=1` 默认关。③ **worktree_sandbox 待接**（诚实排期）：落点=Orchestrator/subloop 子代理工作树分配（spawn 前建/收束后删 + TDD 状态机），涉子代理生命周期单独批做实。④ 契约纠偏留档：`ToolCall/ToolResult` 在 **protocol** 非 plugin、impl 必须 `#[async_trait]`（漏宏=生命期错） | 2026-10-10 亲跑：tools+assembly+cli **437 passed / 0 failed**（新增 4 测：morphology 默认关不变/浅收紧深回满+上限封顶、education 真 Dx-Check 效果可见/缺参拒绝）；clippy `-D warnings` 0 警告 | `cargo test -p apeireth-runtime-assembly --lib morphology`；`cargo test -p apeireth-tools-canonical --lib education`；旋钮复现：`APEIRETH_ENABLE_MORPHOLOGY_RECALL=1` / `APEIRETH_ENABLE_EDUCATION=1` |

## 2. 挂账（未测 / 测不了）——不要声称已验

| # | 项 | 原因 | 若要做时的路径 |
|---|---|---|---|
| 1 | **MiniMax provider 真机** | 无 MiniMax key（用户侧无预算） | `#[ignore]` 测试齐备：`minimax_llm_factory::real_llm_call_smoke` 等，有 key 后 `cargo test -p apeireth-provider --test minimax_llm_factory -- --ignored` + env `MINIMAX_API_KEY` |
| 2 | **桌面 UI 点击流人工实测** | 🟡 核心链路已人工走通（2026-09-28 用户实测：装机→启动→设置填 key→保存→网关重启→真实流式对话出字，全程抓出 5 个真 bug：CWD 启动失败 / CORS 缺失 / 密钥弹窗不推侧车 / 保存按钮不可见 / 错误帧被吞）；**剩余人工步骤**：① 工具面板 shell 审批闭环（开 shell 旋钮→触发→批准，现审批卡已显示命令文本）② 图形卸载勾选"删除应用程序数据"验证数据目录真删 ③ 设置页钥匙串保存→重启后 key 仍在（P0-1 GUI 路径）④ 会话模型/预设选择器 + 斜杠菜单的点击流 | 清单：`frontend/companion-desktop/docs/first-run-click-through-checklist.md`；观察日志 `%LOCALAPPDATA%…/logs/apeireth-backend.log` |
| 3 | `/v1/apeireth/events` 订阅端到端（桌面 UI 里收事件） | 端点已确认是活流（探针连接保持），UI 消费未人工验证 | presence 订阅代码在 `presence.ts`；UI 验证并入 #2 |
| 4 | approvals 的 HTTP 完整闭环 | 完整闭环在 CLI 实测过（#3）；HTTP 路由只验了参数校验响应 | HTTP 闭环可并入 #2（工具触发 → 面板审批按钮） |
| 5 | macOS / Linux 打包与装机 | 仅 Windows NSIS 装机实测 | Tauri bundle 命令已有，缺真机验证环境 |
| 6 | MSI 卸载与 NSIS 对齐（侧车检查） | WiX 模板无此 hook，Windows 推荐 NSIS | 若 MSI 变主力分发，需 WiX CustomAction |
| 7 | RC-7 非文本感知（voice/screen） | 待硬件 | ROADMAP P7/P-arch-3 |
| 8 | **语义向量阶段 live E2E（真 embedding 端点）** | 无可用 embeddings 端点：DeepSeek 不提供 `/embeddings`，本地端点未部署——`OpenAiCompatibleEmbeddingProvider` 只有库级测试 + fake provider 效果测试（T9）过 | 有端点后：`$env:APEIRETH_EMBEDDING_URL=…; $env:APEIRETH_EMBEDDING_MODEL=…` 起 gateway 真聊一轮，观察 recall `retrieval_status.used_lexical_fallback == false` 且 `vector_candidates > 0` |
| 9 | **provider/organ live E2E 重跑**（openai_compatible_live 2 支 + organ_live_llm 2 支） | 2026-10-06 实跑时 **DeepSeek key 尾号 3d17 已被官方撤销**（vendor 401）——测试本身诚实红，非测试缺陷；council_live 假绿洞已封（#44） | 主人在 DeepSeek 控制台换新 key 并存入桌面设置（钥匙串）后：`$env:OPENAI_API_KEY='<新key>'; $env:APEIRETH_OPENAI_URL='https://api.deepseek.com/v1'; $env:APEIRETH_OPENAI_MODELS='deepseek-v4-flash'` 重跑两支 `-- --ignored` |

## 3. 环境口径（live 测试统一契约）

```powershell
# DeepSeek（openai-compatible，唯一实测路径）
$env:OPENAI_API_KEY='…'                 # 每次会话新给；不进 repo、不进 commit（secret-scan 守门）
$env:APEIRETH_OPENAI_URL='https://api.deepseek.com/v1'
$env:APEIRETH_OPENAI_MODELS='deepseek-v4-flash'
```

- key 卫生：DeepSeek key 在聊天记录出现过后应轮换；测试脚本只从 env 读 key。
- 无需 key 的回归：装机 E2E 里除"真聊天"外全部步骤 + lifecycle 测试（#12）在 CI 无 key 环境可跑。

## 4. 更新规则

- 新实测过 → 移到 §1 并填"何时/证据/复现命令"；挂账解除 → 从 §2 删行。
- 只写"当时真跑了"的事实；探针脚本失败/输出存疑 = 不算实测，留在挂账。
- 涉及 commit 的写 commit 短 hash；涉及测试文件的写 `file::test_name`。
