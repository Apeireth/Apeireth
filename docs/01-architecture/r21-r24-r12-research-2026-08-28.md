# R12 OrganOrchestrator 收尾真账 + R21-R24 DEFERRED slot 调研 (2026-08-28)

**作者**: Sub-Agent (主代理 Mavis 派)
**用途**: 给主代理 R12 收尾验证 + R21-R24 派单决策参考
**真账**: 本调研 ≤ 4h, 不真实施, 不 git add/commit/push, 仅写本真账 doc

```
[Document-Meta]
Document:        docs/01-architecture/r21-r24-r12-research-2026-08-28.md
Version:         Research-1.0
Status:          🟢 真账 (调研)
```

---

## 0. TL;DR — 3 项关键真账

1. **R12 OrganOrchestrator 真实施 = 已完成, 不需 readiness 调研**. per `organ-orchestrator-completion-plan.md:204-210`, A 块 5 stage 全部 done + push (commits `fc159288` / `ea9aa14f` / `ed6353f4` / `1972b040` / `edc98170` amended), 1739 tests / 0 clippy / 0 LOCKED 触碰. .harness-msg/<1-5>.txt = O-6 三阶审查追溯文档, 不是待实施 spec.
2. **R15 spec §5.1 L364 critic 路径错账**: v1 实际是 `critic.rs` (CRITIC arXiv 2305.11738 反幻觉, 272 行), 不是 `judge.rs` 也不是 `judicator.rs` (daemon.LlmJudicator 宪法评审 137 行, 是另一物). R21 接力时必用 `critic.rs`.
3. **R23 (planner) / R24 (orchestrator) v1 = 0 真实现** — grep v1 全 `*.rs` 仅 `hypothesis.rs:216 PlannerConfig` 配置, 无 Planner/Orchestrator struct/trait/module. R23/R24 必走"新设计 + LLM Adapter + 0 装诚实"路径, 不走 1:1 翻译.

---

## Part 1: R12 OrganOrchestrator 收尾真账 (不 readiness, 是已落地)

### 1.1 5 stage 草稿可用性真账

| Stage | 草稿 (`.harness-msg/N.txt`) | O-6 三阶审查 | 拒 alternatives | 真实状态 |
|---|---|---|---|---|
| 1 (缺口 D) | 20 行 齐 | 3 阶全齐 | 拒 A 先做 / 放 orchestration / 放 governance / 内部字段 / generic / Result-error-enum | ✅ commit `fc159288` |
| 2 (缺口 B) | 19 行 齐 | 3 阶全齐 | 拒 A 先做 / 跨 OrganTrait / 改 OrganOutput schema / chain 内部 | ✅ commit `ea9aa14f` |
| 3 (缺口 A) | 24 行 齐 | 3 阶全齐 | 拒 downcast / trait method / 留 emergence / parallel e7 字段 | ✅ commit `ed6353f4` |
| 4 (缺口 C) | 27 行 齐 | 3 阶全齐 | 拒 legacy Council / duplicate API / generic / builder | ✅ commit `1972b040` |
| 5 (缺口 E) | 35 行 齐 | 3 阶全齐 | 拒 governance/runtime/orchestration / 寄生 / 调 git / 6 步并行 / L4 完整 pipeline | ✅ commit `edc98170` (amended from `24d163ff`) |

5 stage 单独 commit (不是 1 batch, per plan doc §2). 草稿 = O-6 修订版 commit msg 追溯 (初版 sections 描述 WHAT 不是 WHY, 主代理 O-6 复盘 amend).

### 1.2 0 触碰 LOCKED 验证 (主代理亲跑)

```powershell
# 1. 8 哲学锚本体
git diff b9026186..HEAD -- crates/foundation/core/src/eight_anchors.rs
# 期望: 0 行
# 2. 13 键
git diff b9026186..HEAD -- crates/foundation/core/src/philosophy.rs
# 期望: 0 行 (RUNTIME_ENFORCED 0 改)
# 3. 3 不可变脊柱
git diff b9026186..HEAD -- crates/foundation/core/src/onion.rs
# 期望: 0 行 (仅 rustfmt)
# 4. workspace.version
git diff b9026186..HEAD -- Cargo.toml | Select-String "version"
# 期望: workspace.version = "1.2.0" 0 改
# 5. R11 baseline (per release-path §3.3)
git diff b9026186..HEAD -- crates/ | Select-String "0.8682|0.8532|0.9063"
# 期望: 0 行
```

### 1.3 R12 估时真账

- plan §2 估 16-21h (~3-4 工作日), 5 stage 已实施, 1739 tests (1726 + 13 新)
- 阻 R12 的真问题 = 0 (R12 = R11 spec 真实施, 9 organ done 后即可)
- R12 不占估时, 主代理 R12 收尾 = 5 项 grep 验证 + 5 min 复盘

---

## Part 2: R21-R24 DEFERRED slot 调研真账

### 2.1 5 slot 现状 + v1 真实现源 (per ledger L31-35)

| Slot | Ledger 状态 | v1 真实现源 | 1:1 翻译 | 估时 |
|---|---|---|---|---|
| `cognitive.critic` | DEFERRED INTO JUDGE | `critic.rs` (272 行, CRITIC 反幻觉) | ✅ 1:1 | 1 周 |
| `cognitive.reflection` | DEFERRED INTO SELF-ASSESSMENT | `reflection.rs` (329 行, 4 阶段 cycle) | ✅ 1:1 | 1 周 |
| `cognitive.planner` | NOT AN AGENT MODULE | **v1 0** (仅 `hypothesis.rs:216 PlannerConfig`) | ❌ 新建 + LLM Adapter | 3 周 |
| `cognitive.orchestrator` | NOT AN AGENT MODULE | **v1 0** (无 Orchestrator struct/trait) | ❌ 新建 + LLM 类 AwakeCompanion | 3 周 |
| `cognitive.perception` | NOT AN AGENT MODULE | `screen_perception.rs` (5.7KB) + `observer_capture.rs` (24KB) + `voice_session.rs` (6KB) | ⚠ 部分 1:1 (screen 1:1; voice/capture 需硬件) | 2-3 周 |

### 2.2 R21-R24 spec 接力路径真账 (仿 R15 §5.5 模板)

**R21 (cognitive.critic)**: v1 `critic.rs` 3 块真账 = `extract_claims` (行级启发, 非 LLM 语义) + `ClaimVerifier` trait (宿主注入) + `ReflectionCritic` 组合器. 0 装诚实 = 无法验证如实标 `Unverifiable`. R21 spec 必含 6 节 + 5 项 LOCKED + 0 装诚实 4 块 + **路径修正 = `critic.rs` 不是 R15 §5.1 标的 `judge.rs`**.

**R22 (cognitive.reflection)**: v1 `reflection.rs` 4 阶段 cycle (Triggered→Reflecting→Consolidating→Concluded). 3 块 = `ReflectionScheduler` (真写回 SQLite 【反思周期】episode) + `ReflectionReflector` trait (lib 0 LLM 依赖, 同做梦摘要 trait 策略; 未接 = 诚实降级) + N4 元自学习 thought_reader. R22 spec 必含 6 节模板.

**R23 (cognitive.planner) — 不走 1:1 翻译**: v1 = 0 真实现. R23 spec 必含新设计 + LLM Adapter 模式 + 0 装诚实标. 3 候选 LLM 接入: `ModuleInvoker` adapter (per ledger L69 "All current cognitive side-calls use the existing runtime-owned `ModuleInvoker`") / `LlmFactory` adapter (per ledger L67) / AwakeCompanion 类比. R23 spec 不假装"v2 cognitive.planner 是 v1 1:1 翻译".

**R24 (cognitive.orchestrator) — 不走 1:1 翻译 + 跟 R12 区分**: v1 = 0 真实现. R24 必明确区分 (per R15 §5.1 L367): **R12 OrganOrchestrator** = tick 层 runtime composition root (L0-L5 串行调度 + 9 organ 串联); **A 块 UpgradeCycle** = meta-orchestration (commit `edc98170`, 400 行, L0 governance → L1 self_assessment → L2 council → L3 chain_9_organs → L4 governance → L5 tag suggestion); **R24 cognitive.orchestrator** = cognitive slot adapter (NOT AN AGENT MODULE per ledger L34) + LLM 类 AwakeCompanion Planner→Implementer→Reviewer 3 阶段 service. R24 spec 必含新设计 + **跟 R12/UpgradeCycle 严格分界真账** + v1 0 实现真账.

**R14 (cognitive.perception)**: R14 RC-7 spec 写中 (572 行, per release-path L134), 硬件依赖 Whisper (voice) + xcap (screen capture). 本调研不重复.

### 2.3 0 触碰 LOCKED 预测 (R21-R24 每 slot)

| Slot | LOCKED 触碰预测 | OrganKind variant |
|---|---|---|
| R21 critic | 新 crate `crates/engine/critic/` + 1 新 variant `OrganKind::Critic` (R10 决策) | 加新 variant, 0 改 9 现有 |
| R22 reflection | 新 crate `crates/engine/reflection/` + 1 新 variant | 加新 variant, 0 改 9 现有 |
| R23 planner | 新 crate `crates/engine/planner/` + 1 新 variant + LLM Adapter trait | 加新 variant, 0 改 9 现有 |
| R24 orchestrator | 跟 R12/A 块 UpgradeCycle 严格分界, cognitive slot adapter = thin shim | 加新 variant, 0 改 9 现有 |
| R14 perception | 新 backend `crates/engine/perception/` + PerceptionBackend trait | 0 改 organ trait |

净触碰 = 6 新 sub-crate (含 preference_learning) + 5 新 OrganKind variant (R10 spec 决策点). **0 触碰** 9 哲学锚 / 13 键 / 3 脊柱 / workspace.version / R11 baseline / cognitive.rs 12 slot.

### 2.4 派单顺序真账 (per release-path §3.4 + R15 §5.5)

| 顺序 | 子代理 | slot | 估时 | 依赖 | 理由 |
|---|---|---|---|---|---|
| 1 | R20 | preference_learning | 2 周 | R12 ✅ + R10 variant | R15 spec done (617 行), 1:1 翻译最清晰 |
| 2 | R21 | critic | 1 周 | R12 ✅ | 1:1 翻译, 风险低 |
| 3 | R22 | reflection | 1 周 | R12 ✅ | 1:1 翻译, 跟 R21 并行 0 冲突 |
| 4 | R14 | perception | 2-3 周 | 硬件 | 独立子目录, 跟 R21/R22 并行 |
| 5 | R23 | planner | 3 周 | R23 spec (R18 待写) + LLM Adapter | v1 0 实现, 新设计 |
| 6 | R24 | orchestrator | 3 周 | R24 spec (R19 待写) + 跟 R12 区分 | v1 0 实现 + 分界, 最复杂 |

**总估时**: 串行 12-13 周 (不推荐); 并行 **6-10 周** (R20 2 + R21+R22+R14 4 + R23+R24 6).

---

## Part 3: 总真账 + 下步建议

### 3.1 0 装诚实标 (调研 vs 真实施)

| 派单 | 调研 + spec 接力 | 真实施 |
|---|---|---|
| R20 preference_learning | R15 spec done (617 行) + C 块 readiness done (182 行) | 主代理后续派 2 周 |
| R21 critic | **R16 spec 待写** (R15 §5.1 critic 路径错账已 flag) | 1 周真实施 |
| R22 reflection | **R17 spec 待写** | 1 周真实施 |
| R23 planner | **R18 spec 待写** (v1 0 实现真账已 flag) | 3 周真实施 |
| R24 orchestrator | **R19 spec 待写** (跟 R12/UpgradeCycle 分界真账已 flag) | 3 周真实施 |
| R14 perception | R14 RC-7 spec 写中 | 2-3 周硬件 |

**本调研 0 装诚实真账**:
- ✅ 本调研 ≤ 4h 写 1 真账 doc
- ❌ 不真写 R16/R17/R18/R19 spec (估各 30-45 分钟, 后续派)
- ❌ 不真做 R20-R24 真实施 (估 6-10 周, 后续派)
- ❌ 不真跑 5 重守门 (主代理亲跑, 本调研仅列命令)
- ✅ R15 spec §5.1 critic 路径错账已 flag

### 3.2 commit message 模板

**R12 5 stage commit msg** (.harness-msg 已写, 已 amend 真实施):
```
refactor(runtime): OrganOrchestrator 完整化 stage N — <一句话>

- 0 装诚实真账: <tests 数字 + 0 clippy + 0 LOCKED + Cargo.lock 0 diff>
- O-6 三阶审查:
  - 总体最优: <release 语境 + 拒 alternatives>
  - 系统最优: <子 crate 位置 + 拒 alternatives>
  - 架构最优: <公开 API + 拒 alternatives>
- 影响面: <文件清单>
- 后续 stage: <链式依赖>
```

**R21-R24 spec 接力 commit msg** (仿 R15):
```
docs(architecture): R{21-24} spec 接力 R15 — <slot 名>

- 0 装诚实真账: <R{21-24} 30-45 分钟 spec, 不真做 1-3 周>
- v1 真实现源: legacy/donor/apeireth-companion/src/{critic|reflection}.rs (file:line)
- v2 真实施路径: <新 crate + OrganTrait + 0 LLM 决策>
- 接力 spec 必含: 6 节 + 5 项 LOCKED + 0 装诚实 4 块
- 0 触碰 LOCKED 5 项
- 后续派单: R{20-24} 真实施估时 + 并行计划
```

**R20-R24 真实施 commit msg** (仿 A 块 stage 1-5):
```
refactor(engine): <slot 名> 真实施 — <一句话>

- 0 装诚实真账: <tests + 0 clippy + 0 LOCKED + Cargo.lock 0 diff>
- 1:1 翻译 v1 <file>: <file:line + 字段 1:1 映射>
- OrganTrait::process: <签名 + 0 LLM 决策>
- 集成 cognitive module: <slot 注册 + hook 决策>
- 集成测试: <n 单元 + m 集成 = X tests>
- 0 触碰 LOCKED 5 项
- 后续 commit: <依赖链>
```

### 3.3 给主代理 3 项决策建议

1. **R12 收尾 = 已落地, 不需派单**. 主代理亲跑 Part 1.2 5 项 grep 验证 (5 min). 若核验不通过, 走 git revert / fix forward, 跟 A 块 O-6 复盘同模式.

2. **R20 (preference_learning) 优先派单**. R15 spec 路径已写 + C 块 readiness 已核 + 1:1 翻译最清晰, 风险最低. 任务 brief 必含: ledger L30 DEFERRED→WIRED 是 1 行 doc sync (R15 §7.2 "0 改 ledger" vs C 块 readiness §6 真账冲突, R20 实施前主代理在 spec §7.2 加 1 行标) + R11 Episode 字段预读 (`crates/runtime/...` 或 `crates/foundation/core/src/episode.rs`) + commit msg 4 项标.

3. **R21-R24 派单按 Part 2.4 真账表**. R21/R22/R14 并行 (估 4 周), R23/R24 并行 (估 6 周), 总估 6-10 周真实施. R16-R19 spec 接力 sub-agent 待派 (每 30-45 分钟, 仿 R15 模板). **R18/R19 spec 必含 v1 0 实现真账 + 跟 R12/A 块 UpgradeCycle 分界真账**.

### 3.4 风险真账

- **R1 (R15 §5.1 critic 路径错账, 已 flag)**: R21 spec 用 `critic.rs`, R15 amend 同步改 §5.1.
- **R2 (R23/R24 v1 0 实现, 已 flag)**: R18/R19 spec 含"v1 0 实现真账 + 新设计 + 0 1:1 翻译", 不假装 Planner/Orchestrator 是 v1 1:1 翻译.
- **R3 (R24 跟 R12/A 块 UpgradeCycle 边界, 已 flag)**: R19 spec 含 1 节"cognitive.orchestrator 跟 R12 OrganOrchestrator + A 块 UpgradeCycle 严格分界", cognitive.orchestrator = cognitive slot adapter (NOT AN AGENT MODULE per ledger L34), 不是 tick 层 runtime composition root, 也不是 meta-orchestration.
- **R4 (5 接力 spec 写中)**: 主代理后续派 R16/R17/R18/R19 + R14 真实施, 每 spec 30-45 分钟, 6 真实施估 6-10 周并行. R10 OrganKind 决策 = 新 variant 加 (加不破 9 现有).
- **R5 (本调研 0 触碰 LOCKED 真账)**: 本调研仅写 1 新真账 doc, 0 触碰 LOCKED 文件 (9 哲学锚 / 13 键 / 3 脊柱 / workspace.version / R11 baseline 全 0 改).

---

_调研收尾: **R12 已落地 (A 块 5 stage done + 1739 tests); R21-R24 真账 = 1 处 critic 路径错账已 flag + R23/R24 v1 0 实现真账 + R24 跟 R12/UpgradeCycle 分界真账 + 派单顺序 (R20→R21/R22/R14 并行→R23/R24 并行) 真账写完**. 下一步 = 主代理亲跑 Part 1.2 5 项 grep 验证 R12 收尾 + 派 R20 真实施 preference_learning (R15 spec 接力, 估 2 周)._
