# Capabilities

Capabilities represent actions exposed to the model/runtime registry. They are
governed through canonical Governance and plugin contracts. External I/O must
use canonical enforcement boundaries (for example controlled egress).

Contents: tools (package name `apeireth-tools-canonical`).

