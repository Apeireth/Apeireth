# Modules

Modules are product features built on the canonical engine. They may not
redefine foundation contracts or duplicate runtime orchestration.

This directory is reserved for future canonical product modules. Historical
Companion code is retained under `legacy/donor/apeireth-companion` and is not a
current workspace member.

