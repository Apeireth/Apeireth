# Apeireth 2.0 架构全景白皮书

## 一、 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          1. 呈现与客户端交互层                              │
│         apeireth-cli / TUI            │  Desktop (Tauri 2 + Svelte 5)       │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │ HTTP / WebSocket / SSE
┌──────────────────────────────────────▼──────────────────────────────────────┐
│                          2. 网关与出入站治理层                              │
│                         `apeireth-gateway`                                  │
│  - REST API (/v1/chat/completions, /v1/apeireth/*)                          │
│  - WebSocket 8-Frame Protocol                                               │
│  - S4 Outbound Default-Deny 网关代理 (Domain/IP 白名单物理拦截)             │
│  - MCP (Model Context Protocol) JSON-RPC 2.0 宿主                           │
│  - 全双工 Presence 事件流广播器                                             │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────┐
│                          3. 伴侣智能与器官中枢                              │
│                        `apeireth-companion`                                 │
│  - 涌现循环 (Emergence E7, 8 门禁, 作息学习, Borbély 睡眠压力)              │
│  - 情感与意识 (Plutchik 8 维情绪, PAD 空间, 语气与措辞调制)                 │
│  - 世界模型 (W1 文本模拟器, W2 因果图 MCTS 推演, W3 反事实)                 │
│  - 意图预测与自校准 (W6 Intent Brier Score, 滑动窗口诊断)                   │
│  - 自主好奇心引擎 (E4, 记忆回声偏置)                                        │
│  - TP34 端到端流式 CoT 实时分离与工具回灌状态机                             │
└───────────────────┬─────────────────────────────────────┬───────────────────┘
                    │                                     │
┌───────────────────▼────────────────┐   ┌────────────────▼───────────────────┐
│     4. 统一工具集与沙箱执行        │   │        5. 调度与生命周期运行时     │
│         `apeireth-tools`           │   │         `apeireth-runtime`         │
│  - 插件化 ToolRegistry             │   │  - LivingCycleHeartbeat 日循环     │
│  - 内置工具套件 (Shell/Fetch/FS等) │   │  - AsyncTaskStore (TOCTOU-Safe)    │
│  - Windows JobObject 资源硬限制    │   │  - Supervisor 监督树 (进程自愈)    │
│  - RestrictedToken 最小特权隔离    │   │  - OTel 兼容分布式链路追踪 (Trace) │
└───────────────────┬────────────────┘   └────────────────┬───────────────────┘
                    │                                     │
┌───────────────────▼─────────────────────────────────────▼───────────────────┐
│                          6. 安全、治理与审计层                              │
│                       `apeireth-governance`                                 │
│  - 三重洋葱模型 (Principle / Permission / DSL Guardrails)                   │
│  - 5-Gate 判定与 V1+V2+V3 绝对 AND 门                                       │
│  - 基于属性的权限控制 (ABAC) 引擎                                           │
│  - 主权令牌 (OwnerToken: Master/Admin/Op/RO) & 多签                         │
│  - SHA-256 不可变防篡改审计哈希链 (Tamper-Evident Hash Chain)               │
│  - Self-Disable 防护 (Q20 百年章节, 编译期 hardcode)                        │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────┐
│                          7. 多协议与模型抽象层                              │
│                        `apeireth-protocol`                                  │
│  - NormalizedRequest / NormalizedResponse 统一内部形态                      │
│  - OpenAI Chat & Responses, Anthropic, Gemini, MiniMax 适配器               │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────┐
│                          8. 统一存储与持久化层                              │
│                         `apeireth-storage`                                  │
│  - SQLite 读写分离池 (只读池 + 独占排队写入通道, 0 database locked)         │
│  - 内存高速因果图与实体关联图 (BFS/CRAWL 亚毫秒检索)                        │
│  - Memory v2 (重要度打分, ACT-R 遗忘衰减, 时序有效性, 对账)                 │
│  - 本地向量索引与 BM25 混合召回                                             │
│  - V1-V7 幂等数据库迁移引擎                                                 │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────┐
│                          9. 核心底座与领域实体                              │
│                          `apeireth-core`                                    │
│  - 核心领域模型 (Episode, Note, Session, IdentityCard)                      │
│  - 13 哲学密钥与八大哲学锚                                                  │
│  - 零开销跨模块事件总线 (EventBus)                                          │
│  - 虚拟与真实时钟抽象 (Clock / VirtualClock)                                │
│  - 9 阶段生命周期状态机                                                     │
└─────────────────────────────────────────────────────────────────────────────┘
```
