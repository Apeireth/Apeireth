[package]
name = "apeireth-upgrade"
version = "2.0.0"
edition = "2021"
rust-version = "1.85"
license = "Apache-2.0"
authors = ["Apeireth Team"]
description = "Apeireth 升级器官 (A15 落点 — R14 Phase 5 OTA 升级 + sandbox-validator + 5 重治理)"

[dependencies]
apeireth-core = { path = "../apeireth-core" }
# round10-10 跨 crate 集成: OTA 3 阶段接入外部 governance 守门 (v2 自包含实现, 不依赖 council/sovereignty/constraint)
serde = { workspace = true }
serde_json = { workspace = true }
async-trait = { workspace = true }
tracing = "0.1"
anyhow = { workspace = true }
thiserror = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lib]
name = "apeireth_upgrade"
path = "src/lib.rs"

[[example]]
name = "upgrade_demo"
path = "examples/upgrade_demo.rs"

[lints]
workspace = true
