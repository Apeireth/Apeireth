[package]
name = "apeireth-extension"
version = "2.0.0"
edition = "2021"
rust-version = "1.85"
authors = ["Apeireth Team"]
license = "Apache-2.0"
repository = "https://github.com/apeireth/apeireth"
description = "apeireth-extension — 6 类插件 (sync / async / static / service / messagePreprocessor / hybrid) + extension.toml 严格 schema + 审核后注册 + 沙盒 + 调用审计. v2 style rewrite with v1 pub API surface 100% covered."
homepage = "https://github.com/apeireth/apeireth"
keywords = ["plugin", "extension", "sandbox", "audit"]
categories = ["api-bindings"]

[dependencies]
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
async-trait = { workspace = true }
tokio = { workspace = true }
once_cell = "1.18"

[lints]
workspace = true
