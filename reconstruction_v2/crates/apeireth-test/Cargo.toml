[package]
name = "apeireth-test"
version = "2.0.0"
edition = "2021"
rust-version = "1.85"
authors = ["Apeireth Team"]
license = "Apache-2.0"
repository = "https://github.com/apeireth/apeireth"
description = "apeireth-test — Test harness (TestCase + flaky detection + property tests) (v2 port of v1.0-legacy)"
homepage = "https://github.com/apeireth/apeireth"
keywords = ["ai"]
categories = ["development-tools::testing"]

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }

[dev-dependencies]
proptest = "1.5"

[lints]
workspace = true